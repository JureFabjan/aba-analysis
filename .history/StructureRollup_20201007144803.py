
class StructureRollup:
    
    #def __init__(self)

    def by(columns, aggregations):
        # data.groupby([col for col in data.columns if 'level_' in col])['expression_level','z-score'].agg(aggregations)
        
    